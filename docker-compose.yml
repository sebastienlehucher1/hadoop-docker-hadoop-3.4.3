# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.


version: "3.9"

services:

  namenode:
    build:
      context: .
      dockerfile: Dockerfile.hadoop
    container_name: namenode
    hostname: namenode
    command: ["bash", "-c", "sudo mkdir -p /hadoop/dfs/name && sudo chown -R hadoop:users /hadoop/dfs/name && if [ ! -d /hadoop/dfs/name/current\
        \ ]; then hdfs namenode -format -force; fi && hdfs namenode & sleep 5 && /namenode-init.sh &&\
        \ wait"
    ]    
    ports:
      - 9870:9870   # HDFS UI
      - 8020:8020   # HDFS RPC
    env_file:
      - ./config      
    volumes:
      - .:/workspace
      - ./api_key.txt:/run/secrets/api_key:ro
      - ./hadoop-config:/etc/hadoop/conf
      - ./namenode-init.sh:/namenode-init.sh
      - ./scripts:/opt/hadoop/scripts
      - namenode_data:/hadoop/dfs/name
      - tez-share:/tez
    healthcheck:
      test: ["CMD-SHELL", "hdfs dfsadmin -report | grep -q 'Live datanodes (1)'"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 60s
    networks:
      - hadoop

  datanode:
    build:
      context: .
      dockerfile: Dockerfile.hadoop
    container_name: datanode
    hostname: datanode
    command: ["bash", "-c", "sudo mkdir -p /hadoop/dfs/data && sudo chown -R hadoop:users /hadoop/dfs/data && hdfs datanode"]    
    env_file:
      - ./config
    ports:
      - 9864:9864
    volumes:
      - datanode_data:/hadoop/dfs/data
    depends_on:
      namenode:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "jps | grep -q DataNode"]
      interval: 20s
      timeout: 10s
      retries: 10
    networks:
      - hadoop

  resourcemanager:
    build:
      context: .
      dockerfile: Dockerfile.hadoop
    container_name: resourcemanager
    hostname: resourcemanager
    command: ["yarn", "resourcemanager"]
    ports:
        - 8088:8088
    env_file:
      - ./config
    volumes:
      - ./test.sh:/opt/test.sh
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:8088/ws/v1/cluster/info >/dev/null"]
    networks:
      - hadoop

  nodemanager:
    build:
      context: .
      dockerfile: Dockerfile.hadoop
    container_name: nodemanager
    hostname: nodemanager
    command: ["yarn", "nodemanager"]
    env_file:
      - ./config
    healthcheck:
      test: ["CMD-SHELL", "jps | grep -q NodeManager"]
    networks:
      - hadoop

  hive-postgres:
    image: postgres:14
    container_name: hive-postgres
    hostname: hive-postgres
    env_file: .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    ports:
      - 5432:5432
    volumes:
      - metastore_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - hadoop

  hive-metastore:
    build:
      context: .
      dockerfile: Dockerfile.hive
    container_name: hive-metastore
    hostname: hive-metastore
    command: ["/bin/bash", "/start-metastore.sh"]
    env_file: .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      SERVICE_NAME: metastore
      HADOOP_CONF_DIR: /etc/hadoop/conf
      HIVE_CONF_DIR: /opt/hive/conf     
      DB_DRIVER: postgres   
    ports:
      - 9083:9083
    depends_on:
      namenode:
        condition: service_healthy
      hive-postgres:
        condition: service_healthy
    volumes:
      - ./conf:/opt/hive/conf
      - ./hadoop-config:/etc/hadoop/conf      
      - ./start-metastore.sh:/start-metastore.sh
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 9083"]
      interval: 15s
      timeout: 5s
      retries: 20
    networks:
      - hadoop 

  hive-server:
    build:
      context: .
      dockerfile: Dockerfile.hive
    container_name: hive-server
    hostname: hive-server
    env_file: .env
    entrypoint: ["/bin/bash", "/start-hiveserver2.sh"]
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      SERVICE_NAME: hiveserver2
      HADOOP_CONF_DIR: /etc/hadoop/conf
      HIVE_CONF_DIR: /opt/hive/conf     
      DB_DRIVER: postgres      
    ports:
      - 10000:10000  # JDBC/Beeline
      - 10002:10002  # Web UI
    depends_on:
      namenode:
        condition: service_healthy
      datanode:
        condition: service_healthy
      nodemanager:
        condition: service_healthy
      resourcemanager:
        condition: service_healthy
      hive-postgres:
        condition: service_healthy
      hive-metastore:
        condition: service_healthy
    volumes:
      - ./conf:/opt/hive/conf
      - ./hadoop-config:/etc/hadoop/conf
      - ./logs/hive-server:/opt/hive/logs
      - ./start-hiveserver2.sh:/start-hiveserver2.sh
      - tez-share:/tez
    networks:
      - hadoop

volumes:
  namenode_data:
  datanode_data:
  metastore_db_data:
  tez-share:

networks:
  hadoop:
    external: true
    name: hadoop